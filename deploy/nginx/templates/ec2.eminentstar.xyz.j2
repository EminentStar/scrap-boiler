server{
        listen 80;
        server_name {{ domain }};

        location /static {
                alias /home/ubuntu/static;
        }

        location / {
                proxy_pass http://unix:/home/ubuntu/url-shortener-jk/urlshortener.sock;
                #proxy_pass http://localhost:8000;
        }

}
